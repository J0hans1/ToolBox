import { deleteAd } from "../lib/controller";
import { Ad } from "../types/types";

/* 
interface AdsProps{
    ad: Ad;
}

function deleteAdButton(ad: string){

    if (ad != null){
        const x = deleteAd(ad);
        console.log(x);
    } 
}

export default function InformationAds(props: AdsProps){
    return (
        <div className="addsInfo">
            <br />
            <h2>{props.ad.price}</h2>
            <h2>{props.ad.title}</h2>

            <button onClick={() => deleteAdButton(props.ad.id)}>Delete ad above</button>
        </div>
    )
} */